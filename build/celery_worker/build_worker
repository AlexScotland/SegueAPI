from python:3.6-buster

WORKDIR /worker

COPY build/celery_worker/requirements.txt .

RUN pip install -r requirements.txt

# ADD CELERY COMMAND

CMD ["celery", "-A", "tasks", "worker", "--loglevel=INFO", "--pool=gevent", "--concurrency=500"]
